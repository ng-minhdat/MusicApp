spring.application.name: backend

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/musicdb
    username: root
    password: root
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
